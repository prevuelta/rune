(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
module.exports = require('./lib/draggable');

},{"./lib/draggable":2}],2:[function(require,module,exports){
'use strict';

var React = require('react');
var emptyFunction = function(){};
var assign = require('object-assign');
var classNames = require('classnames');

//
// Helpers. See Element definition below this section.
//

function createUIEvent(draggable) {
  // State changes are often (but not always!) async. We want the latest value.
  var state = draggable._pendingState || draggable.state;
  return {
    node: draggable.getDOMNode(),
    position: {
      top: state.clientY,
      left: state.clientX
    }
  };
}

function canDragY(draggable) {
  return draggable.props.axis === 'both' ||
      draggable.props.axis === 'y';
}

function canDragX(draggable) {
  return draggable.props.axis === 'both' ||
      draggable.props.axis === 'x';
}

function isFunction(func) {
  return typeof func === 'function' || Object.prototype.toString.call(func) === '[object Function]';
}

// @credits https://gist.github.com/rogozhnikoff/a43cfed27c41e4e68cdc
function findInArray(array, callback) {
  for (var i = 0, length = array.length; i < length; i++) {
    if (callback.apply(callback, [array[i], i, array])) return array[i];
  }
}

function matchesSelector(el, selector) {
  var method = findInArray([
    'matches',
    'webkitMatchesSelector',
    'mozMatchesSelector',
    'msMatchesSelector',
    'oMatchesSelector'
  ], function(method){
    return isFunction(el[method]);
  });

  return el[method].call(el, selector);
}

/**
 * simple abstraction for dragging events names
 * */
var eventsFor = {
  touch: {
    start: 'touchstart',
    move: 'touchmove',
    end: 'touchend'
  },
  mouse: {
    start: 'mousedown',
    move: 'mousemove',
    end: 'mouseup'
  }
};

// Default to mouse events
var dragEventFor = eventsFor['mouse'];

/**
 * get {clientX, clientY} positions of control
 * */
function getControlPosition(e) {
  var position = (e.touches && e.touches[0]) || e;
  return {
    clientX: position.clientX,
    clientY: position.clientY
  };
}

function addEvent(el, event, handler) {
  if (!el) { return; }
  if (el.attachEvent) {
    el.attachEvent('on' + event, handler);
  } else if (el.addEventListener) {
    el.addEventListener(event, handler, true);
  } else {
    el['on' + event] = handler;
  }
}

function removeEvent(el, event, handler) {
  if (!el) { return; }
  if (el.detachEvent) {
    el.detachEvent('on' + event, handler);
  } else if (el.removeEventListener) {
    el.removeEventListener(event, handler, true);
  } else {
    el['on' + event] = null;
  }
}

function outerHeight(node) {
  // This is deliberately excluding margin for our calculations, since we are using
  // offsetTop which is including margin. See getBoundPosition
  var height = node.clientHeight;
  var computedStyle = window.getComputedStyle(node);
  height += int(computedStyle.borderTopWidth);
  height += int(computedStyle.borderBottomWidth);
  return height;
}

function outerWidth(node) {
  // This is deliberately excluding margin for our calculations, since we are using
  // offsetLeft which is including margin. See getBoundPosition
  var width = node.clientWidth;
  var computedStyle = window.getComputedStyle(node);
  width += int(computedStyle.borderLeftWidth);
  width += int(computedStyle.borderRightWidth);
  return width;
}
function innerHeight(node) {
  var height = node.clientHeight;
  var computedStyle = window.getComputedStyle(node);
  height -= int(computedStyle.paddingTop);
  height -= int(computedStyle.paddingBottom);
  return height;
}

function innerWidth(node) {
  var width = node.clientWidth;
  var computedStyle = window.getComputedStyle(node);
  width -= int(computedStyle.paddingLeft);
  width -= int(computedStyle.paddingRight);
  return width;
}

function isNum(num) {
  return typeof num === 'number' && !isNaN(num);
}

function int(a) {
  return parseInt(a, 10);
}

function getBoundPosition(draggable, clientX, clientY) {
  var bounds = JSON.parse(JSON.stringify(draggable.props.bounds));
  var node = draggable.getDOMNode();
  var parent = node.parentNode;

  if (bounds === 'parent') {
    var nodeStyle = window.getComputedStyle(node);
    var parentStyle = window.getComputedStyle(parent);
    // Compute bounds. This is a pain with padding and offsets but this gets it exactly right.
    bounds = {
      left: -node.offsetLeft + int(parentStyle.paddingLeft) +
            int(nodeStyle.borderLeftWidth) + int(nodeStyle.marginLeft),
      top: -node.offsetTop + int(parentStyle.paddingTop) +
            int(nodeStyle.borderTopWidth) + int(nodeStyle.marginTop),
      right: innerWidth(parent) - outerWidth(node) - node.offsetLeft,
      bottom: innerHeight(parent) - outerHeight(node) - node.offsetTop
    };
  }

  // Keep x and y below right and bottom limits...
  if (isNum(bounds.right)) clientX = Math.min(clientX, bounds.right);
  if (isNum(bounds.bottom)) clientY = Math.min(clientY, bounds.bottom);

  // But above left and top limits.
  if (isNum(bounds.left)) clientX = Math.max(clientX, bounds.left);
  if (isNum(bounds.top)) clientY = Math.max(clientY, bounds.top);

  return [clientX, clientY];
}

function snapToGrid(grid, pendingX, pendingY) {
  var x = Math.round(pendingX / grid[0]) * grid[0];
  var y = Math.round(pendingY / grid[1]) * grid[1];
  return [x, y];
}

// Useful for preventing blue highlights all over everything when dragging.
var userSelectStyle = ';user-select: none;-webkit-user-select:none;-moz-user-select:none;' +
  '-o-user-select:none;-ms-user-select:none;';

function addUserSelectStyles(draggable) {
  if (!draggable.props.enableUserSelectHack) return;
  var style = document.body.getAttribute('style') || '';
  document.body.setAttribute('style', style + userSelectStyle);
}

function removeUserSelectStyles(draggable) {
  if (!draggable.props.enableUserSelectHack) return;
  var style = document.body.getAttribute('style') || '';
  document.body.setAttribute('style', style.replace(userSelectStyle, ''));
}

function createCSSTransform(style) {
  // Replace unitless items with px
  var x = style.x + 'px';
  var y = style.y + 'px';
  return {
    transform: 'translate(' + x + ',' + y + ')',
    WebkitTransform: 'translate(' + x + ',' + y + ')',
    OTransform: 'translate(' + x + ',' + y + ')',
    msTransform: 'translate(' + x + ',' + y + ')',
    MozTransform: 'translate(' + x + ',' + y + ')'
  };
}


//
// End Helpers.
//

//
// Define <Draggable>
//

module.exports = React.createClass({
  displayName: 'Draggable',

  propTypes: {
    /**
     * `axis` determines which axis the draggable can move.
     *
     * 'both' allows movement horizontally and vertically.
     * 'x' limits movement to horizontal axis.
     * 'y' limits movement to vertical axis.
     *
     * Defaults to 'both'.
     */
    axis: React.PropTypes.oneOf(['both', 'x', 'y']),

    /**
     * `bounds` determines the range of movement available to the element.
     * Available values are:
     *
     * 'parent' restricts movement within the Draggable's parent node.
     *
     * Alternatively, pass an object with the following properties, all of which are optional:
     *
     * {left: LEFT_BOUND, right: RIGHT_BOUND, bottom: BOTTOM_BOUND, top: TOP_BOUND}
     *
     * All values are in px.
     *
     * Example:
     *
     * ```jsx
     *   var App = React.createClass({
     *       render: function () {
     *         return (
     *            <Draggable bounds={{right: 300, bottom: 300}}>
     *              <div>Content</div>
     *           </Draggable>
     *         );
     *       }
     *   });
     * ```
     */
    bounds: React.PropTypes.oneOfType([
      React.PropTypes.shape({
        left: React.PropTypes.Number,
        right: React.PropTypes.Number,
        top: React.PropTypes.Number,
        bottom: React.PropTypes.Number
      }),
      React.PropTypes.oneOf(['parent', false])
    ]),

    /**
     * By default, we add 'user-select:none' attributes to the document body
     * to prevent ugly text selection during drag. If this is causing problems
     * for your app, set this to `false`.
     */
    enableUserSelectHack: React.PropTypes.bool,

    /**
     * `handle` specifies a selector to be used as the handle that initiates drag.
     *
     * Example:
     *
     * ```jsx
     *   var App = React.createClass({
     *       render: function () {
     *         return (
     *            <Draggable handle=".handle">
     *              <div>
     *                  <div className="handle">Click me to drag</div>
     *                  <div>This is some other content</div>
     *              </div>
     *           </Draggable>
     *         );
     *       }
     *   });
     * ```
     */
    handle: React.PropTypes.string,

    /**
     * `cancel` specifies a selector to be used to prevent drag initialization.
     *
     * Example:
     *
     * ```jsx
     *   var App = React.createClass({
     *       render: function () {
     *           return(
     *               <Draggable cancel=".cancel">
     *                   <div>
     *                     <div className="cancel">You can't drag from here</div>
     *            <div>Dragging here works fine</div>
     *                   </div>
     *               </Draggable>
     *           );
     *       }
     *   });
     * ```
     */
    cancel: React.PropTypes.string,

    /**
     * `grid` specifies the x and y that dragging should snap to.
     *
     * Example:
     *
     * ```jsx
     *   var App = React.createClass({
     *       render: function () {
     *           return (
     *               <Draggable grid={[25, 25]}>
     *                   <div>I snap to a 25 x 25 grid</div>
     *               </Draggable>
     *           );
     *       }
     *   });
     * ```
     */
    grid: React.PropTypes.arrayOf(React.PropTypes.number),

    /**
     * `start` specifies the x and y that the dragged item should start at
     *
     * Example:
     *
     * ```jsx
     *      var App = React.createClass({
     *          render: function () {
     *              return (
     *                  <Draggable start={{x: 25, y: 25}}>
     *                      <div>I start with transformX: 25px and transformY: 25px;</div>
     *                  </Draggable>
     *              );
     *          }
     *      });
     * ```
     */
    start: React.PropTypes.shape({
      x: React.PropTypes.number,
      y: React.PropTypes.number
    }),

    /**
     * `moveOnStartChange`, if true (default false) will move the element if the `start`
     * property changes.
     */
    moveOnStartChange: React.PropTypes.bool,


    /**
     * `zIndex` specifies the zIndex to use while dragging.
     *
     * Example:
     *
     * ```jsx
     *   var App = React.createClass({
     *       render: function () {
     *           return (
     *               <Draggable zIndex={100}>
     *                   <div>I have a zIndex</div>
     *               </Draggable>
     *           );
     *       }
     *   });
     * ```
     */
    zIndex: React.PropTypes.number,

    /**
     * Called when dragging starts.
     * If this function returns the boolean false, dragging will be canceled.
     *
     * Example:
     *
     * ```js
     *  function (event, ui) {}
     * ```
     *
     * `event` is the Event that was triggered.
     * `ui` is an object:
     *
     * ```js
     *  {
     *    position: {top: 0, left: 0}
     *  }
     * ```
     */
    onStart: React.PropTypes.func,

    /**
     * Called while dragging.
     * If this function returns the boolean false, dragging will be canceled.
     *
     * Example:
     *
     * ```js
     *  function (event, ui) {}
     * ```
     *
     * `event` is the Event that was triggered.
     * `ui` is an object:
     *
     * ```js
     *  {
     *    position: {top: 0, left: 0}
     *  }
     * ```
     */
    onDrag: React.PropTypes.func,

    /**
     * Called when dragging stops.
     *
     * Example:
     *
     * ```js
     *  function (event, ui) {}
     * ```
     *
     * `event` is the Event that was triggered.
     * `ui` is an object:
     *
     * ```js
     *  {
     *    position: {top: 0, left: 0}
     *  }
     * ```
     */
    onStop: React.PropTypes.func,

    /**
     * A workaround option which can be passed if onMouseDown needs to be accessed,
     * since it'll always be blocked (due to that there's internal use of onMouseDown)
     */
    onMouseDown: React.PropTypes.func,
  },

  componentWillReceiveProps: function(newProps) {
    // React to changes in the 'start' param.
    if (newProps.moveOnStartChange && newProps.start) {
      this.setState(this.getInitialState(newProps));
    }
  },

  componentWillUnmount: function() {
    // Remove any leftover event handlers
    removeEvent(document, dragEventFor['move'], this.handleDrag);
    removeEvent(document, dragEventFor['end'], this.handleDragEnd);
    removeUserSelectStyles(this);
  },

  getDefaultProps: function () {
    return {
      axis: 'both',
      bounds: false,
      handle: null,
      cancel: null,
      grid: null,
      moveOnStartChange: false,
      start: {x: 0, y: 0},
      zIndex: NaN,
      enableUserSelectHack: true,
      onStart: emptyFunction,
      onDrag: emptyFunction,
      onStop: emptyFunction,
      onMouseDown: emptyFunction
    };
  },

  getInitialState: function (props) {
    // Handle call from CWRP
    props = props || this.props;
    return {
      // Whether or not we are currently dragging.
      dragging: false,

      // Offset between start top/left and mouse top/left while dragging.
      offsetX: 0, offsetY: 0,

      // Current transform x and y.
      clientX: props.start.x, clientY: props.start.y
    };
  },

  handleDragStart: function (e) {
    // Make it possible to attach event handlers on top of this one
    this.props.onMouseDown(e);

    // Short circuit if handle or cancel prop was provided and selector doesn't match
    if ((this.props.handle && !matchesSelector(e.target, this.props.handle)) ||
      (this.props.cancel && matchesSelector(e.target, this.props.cancel))) {
      return;
    }

    // Call event handler. If it returns explicit false, cancel.
    var shouldStart = this.props.onStart(e, createUIEvent(this));
    if (shouldStart === false) return;

    var dragPoint = getControlPosition(e);

    // Add a style to the body to disable user-select. This prevents text from
    // being selected all over the page.
    addUserSelectStyles(this);

    // Initiate dragging. Set the current x and y as offsets
    // so we know how much we've moved during the drag. This allows us
    // to drag elements around even if they have been moved, without issue.
    this.setState({
      dragging: true,
      offsetX: dragPoint.clientX - this.state.clientX,
      offsetY: dragPoint.clientY - this.state.clientY
    });


    // Add event handlers
    addEvent(document, dragEventFor['move'], this.handleDrag);
    addEvent(document, dragEventFor['end'], this.handleDragEnd);
  },

  handleDragEnd: function (e) {
    // Short circuit if not currently dragging
    if (!this.state.dragging) {
      return;
    }

    removeUserSelectStyles(this);

    // Turn off dragging
    this.setState({
      dragging: false
    });

    // Call event handler
    this.props.onStop(e, createUIEvent(this));

    // Remove event handlers
    removeEvent(document, dragEventFor['move'], this.handleDrag);
    removeEvent(document, dragEventFor['end'], this.handleDragEnd);
  },

  handleDrag: function (e) {
    var dragPoint = getControlPosition(e);

    // Calculate X and Y
    var clientX = dragPoint.clientX - this.state.offsetX;
    var clientY = dragPoint.clientY - this.state.offsetY;

    // Snap to grid if prop has been provided
    if (Array.isArray(this.props.grid)) {
      var coords = snapToGrid(this.props.grid, clientX, clientY);
      clientX = coords[0], clientY = coords[1];
    }

    if (this.props.bounds) {
      var pos = getBoundPosition(this, clientX, clientY);
      clientX = pos[0], clientY = pos[1];
    }

    // Call event handler. If it returns explicit false, cancel.
    var shouldUpdate = this.props.onDrag(e, createUIEvent(this));
    if (shouldUpdate === false) return this.handleDragEnd();

    // Update transform
    this.setState({
      clientX: clientX,
      clientY: clientY
    });
  },

  onMouseDown: function(ev) {
    // Prevent 'ghost click' which happens 300ms after touchstart if the event isn't cancelled.
    // We don't cancel the event on touchstart because of #37; we might want to make a scrollable item draggable.
    // More on ghost clicks: http://ariatemplates.com/blog/2014/05/ghost-clicks-in-mobile-browsers/
    if (dragEventFor == eventsFor['touch']) {
      return ev.preventDefault();
    }

    return this.handleDragStart.apply(this, arguments);
  },

  onTouchStart: function(ev) {
    // We're on a touch device now, so change the event handlers
    dragEventFor = eventsFor['touch'];

    return this.handleDragStart.apply(this, arguments);
  },

  // Intended for use by a parent component. Resets internal state on this component. Useful for
  // <Resizable> and other components in case this element is manually resized and start/moveOnStartChange
  // don't work for you.
  resetState: function() {
    this.setState({
      offsetX: 0, offsetY: 0, clientX: 0, clientY: 0
    });
  },

  render: function () {
    // Create style object. We extend from existing styles so we don't
    // remove anything already set (like background, color, etc).
    var childStyle = this.props.children.props.style || {};

    // Add a CSS transform to move the element around. This allows us to move the element around
    // without worrying about whether or not it is relatively or absolutely positioned.
    // If the item you are dragging already has a transform set, wrap it in a <span> so <Draggable>
    // has a clean slate.
    var transform = createCSSTransform({
      // Set left if horizontal drag is enabled
      x: canDragX(this) ?
        this.state.clientX :
        0,

      // Set top if vertical drag is enabled
      y: canDragY(this) ?
        this.state.clientY :
        0
    });

    // Workaround IE pointer events; see #51
    // https://github.com/mzabriskie/react-draggable/issues/51#issuecomment-103488278
    var touchHacks = {
      touchAction: 'none'
    };

    var style = assign({}, childStyle, transform, touchHacks);

    // Set zIndex if currently dragging and prop has been provided
    if (this.state.dragging && !isNaN(this.props.zIndex)) {
      style.zIndex = this.props.zIndex;
    }

    var className = classNames((this.props.children.props.className || ''), 'react-draggable', {
      'react-draggable-dragging': this.state.dragging,
      'react-draggable-dragged': this.state.dragged
    });

    // Reuse the child provided
    // This makes it flexible to use whatever element is wanted (div, ul, etc)
    return React.cloneElement(React.Children.only(this.props.children), {
      style: style,
      className: className,

      onMouseDown: this.onMouseDown,
      onTouchStart: this.onTouchStart,
      onMouseUp: this.handleDragEnd,
      onTouchEnd: this.handleDragEnd
    });
  }
});

},{"classnames":3,"object-assign":4,"react":"react"}],3:[function(require,module,exports){
/*!
  Copyright (c) 2015 Jed Watson.
  Licensed under the MIT License (MIT), see
  http://jedwatson.github.io/classnames
*/

function classNames() {
	var classes = '';
	var arg;

	for (var i = 0; i < arguments.length; i++) {
		arg = arguments[i];
		if (!arg) {
			continue;
		}

		if ('string' === typeof arg || 'number' === typeof arg) {
			classes += ' ' + arg;
		} else if (Object.prototype.toString.call(arg) === '[object Array]') {
			classes += ' ' + classNames.apply(null, arg);
		} else if ('object' === typeof arg) {
			for (var key in arg) {
				if (!arg.hasOwnProperty(key) || !arg[key]) {
					continue;
				}
				classes += ' ' + key;
			}
		}
	}
	return classes.substr(1);
}

// safely export classNames for node / browserify
if (typeof module !== 'undefined' && module.exports) {
	module.exports = classNames;
}

// safely export classNames for RequireJS
if (typeof define !== 'undefined' && define.amd) {
	define('classnames', [], function() {
		return classNames;
	});
}

},{}],4:[function(require,module,exports){
'use strict';

function ToObject(val) {
	if (val == null) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

module.exports = Object.assign || function (target, source) {
	var from;
	var keys;
	var to = ToObject(target);

	for (var s = 1; s < arguments.length; s++) {
		from = arguments[s];
		keys = Object.keys(Object(from));

		for (var i = 0; i < keys.length; i++) {
			to[keys[i]] = from[keys[i]];
		}
	}

	return to;
};

},{}],5:[function(require,module,exports){
/*jslint onevar:true, undef:true, newcap:true, regexp:true, bitwise:true, maxerr:50, indent:4, white:false, nomen:false, plusplus:false */
/*global define:false, require:false, exports:false, module:false, signals:false */

/** @license
 * JS Signals <http://millermedeiros.github.com/js-signals/>
 * Released under the MIT license
 * Author: Miller Medeiros
 * Version: 1.0.0 - Build: 268 (2012/11/29 05:48 PM)
 */

(function(global){

    // SignalBinding -------------------------------------------------
    //================================================================

    /**
     * Object that represents a binding between a Signal and a listener function.
     * <br />- <strong>This is an internal constructor and shouldn't be called by regular users.</strong>
     * <br />- inspired by Joa Ebert AS3 SignalBinding and Robert Penner's Slot classes.
     * @author Miller Medeiros
     * @constructor
     * @internal
     * @name SignalBinding
     * @param {Signal} signal Reference to Signal object that listener is currently bound to.
     * @param {Function} listener Handler function bound to the signal.
     * @param {boolean} isOnce If binding should be executed just once.
     * @param {Object} [listenerContext] Context on which listener will be executed (object that should represent the `this` variable inside listener function).
     * @param {Number} [priority] The priority level of the event listener. (default = 0).
     */
    function SignalBinding(signal, listener, isOnce, listenerContext, priority) {

        /**
         * Handler function bound to the signal.
         * @type Function
         * @private
         */
        this._listener = listener;

        /**
         * If binding should be executed just once.
         * @type boolean
         * @private
         */
        this._isOnce = isOnce;

        /**
         * Context on which listener will be executed (object that should represent the `this` variable inside listener function).
         * @memberOf SignalBinding.prototype
         * @name context
         * @type Object|undefined|null
         */
        this.context = listenerContext;

        /**
         * Reference to Signal object that listener is currently bound to.
         * @type Signal
         * @private
         */
        this._signal = signal;

        /**
         * Listener priority
         * @type Number
         * @private
         */
        this._priority = priority || 0;
    }

    SignalBinding.prototype = {

        /**
         * If binding is active and should be executed.
         * @type boolean
         */
        active : true,

        /**
         * Default parameters passed to listener during `Signal.dispatch` and `SignalBinding.execute`. (curried parameters)
         * @type Array|null
         */
        params : null,

        /**
         * Call listener passing arbitrary parameters.
         * <p>If binding was added using `Signal.addOnce()` it will be automatically removed from signal dispatch queue, this method is used internally for the signal dispatch.</p>
         * @param {Array} [paramsArr] Array of parameters that should be passed to the listener
         * @return {*} Value returned by the listener.
         */
        execute : function (paramsArr) {
            var handlerReturn, params;
            if (this.active && !!this._listener) {
                params = this.params? this.params.concat(paramsArr) : paramsArr;
                handlerReturn = this._listener.apply(this.context, params);
                if (this._isOnce) {
                    this.detach();
                }
            }
            return handlerReturn;
        },

        /**
         * Detach binding from signal.
         * - alias to: mySignal.remove(myBinding.getListener());
         * @return {Function|null} Handler function bound to the signal or `null` if binding was previously detached.
         */
        detach : function () {
            return this.isBound()? this._signal.remove(this._listener, this.context) : null;
        },

        /**
         * @return {Boolean} `true` if binding is still bound to the signal and have a listener.
         */
        isBound : function () {
            return (!!this._signal && !!this._listener);
        },

        /**
         * @return {boolean} If SignalBinding will only be executed once.
         */
        isOnce : function () {
            return this._isOnce;
        },

        /**
         * @return {Function} Handler function bound to the signal.
         */
        getListener : function () {
            return this._listener;
        },

        /**
         * @return {Signal} Signal that listener is currently bound to.
         */
        getSignal : function () {
            return this._signal;
        },

        /**
         * Delete instance properties
         * @private
         */
        _destroy : function () {
            delete this._signal;
            delete this._listener;
            delete this.context;
        },

        /**
         * @return {string} String representation of the object.
         */
        toString : function () {
            return '[SignalBinding isOnce:' + this._isOnce +', isBound:'+ this.isBound() +', active:' + this.active + ']';
        }

    };


/*global SignalBinding:false*/

    // Signal --------------------------------------------------------
    //================================================================

    function validateListener(listener, fnName) {
        if (typeof listener !== 'function') {
            throw new Error( 'listener is a required param of {fn}() and should be a Function.'.replace('{fn}', fnName) );
        }
    }

    /**
     * Custom event broadcaster
     * <br />- inspired by Robert Penner's AS3 Signals.
     * @name Signal
     * @author Miller Medeiros
     * @constructor
     */
    function Signal() {
        /**
         * @type Array.<SignalBinding>
         * @private
         */
        this._bindings = [];
        this._prevParams = null;

        // enforce dispatch to aways work on same context (#47)
        var self = this;
        this.dispatch = function(){
            Signal.prototype.dispatch.apply(self, arguments);
        };
    }

    Signal.prototype = {

        /**
         * Signals Version Number
         * @type String
         * @const
         */
        VERSION : '1.0.0',

        /**
         * If Signal should keep record of previously dispatched parameters and
         * automatically execute listener during `add()`/`addOnce()` if Signal was
         * already dispatched before.
         * @type boolean
         */
        memorize : false,

        /**
         * @type boolean
         * @private
         */
        _shouldPropagate : true,

        /**
         * If Signal is active and should broadcast events.
         * <p><strong>IMPORTANT:</strong> Setting this property during a dispatch will only affect the next dispatch, if you want to stop the propagation of a signal use `halt()` instead.</p>
         * @type boolean
         */
        active : true,

        /**
         * @param {Function} listener
         * @param {boolean} isOnce
         * @param {Object} [listenerContext]
         * @param {Number} [priority]
         * @return {SignalBinding}
         * @private
         */
        _registerListener : function (listener, isOnce, listenerContext, priority) {

            var prevIndex = this._indexOfListener(listener, listenerContext),
                binding;

            if (prevIndex !== -1) {
                binding = this._bindings[prevIndex];
                if (binding.isOnce() !== isOnce) {
                    throw new Error('You cannot add'+ (isOnce? '' : 'Once') +'() then add'+ (!isOnce? '' : 'Once') +'() the same listener without removing the relationship first.');
                }
            } else {
                binding = new SignalBinding(this, listener, isOnce, listenerContext, priority);
                this._addBinding(binding);
            }

            if(this.memorize && this._prevParams){
                binding.execute(this._prevParams);
            }

            return binding;
        },

        /**
         * @param {SignalBinding} binding
         * @private
         */
        _addBinding : function (binding) {
            //simplified insertion sort
            var n = this._bindings.length;
            do { --n; } while (this._bindings[n] && binding._priority <= this._bindings[n]._priority);
            this._bindings.splice(n + 1, 0, binding);
        },

        /**
         * @param {Function} listener
         * @return {number}
         * @private
         */
        _indexOfListener : function (listener, context) {
            var n = this._bindings.length,
                cur;
            while (n--) {
                cur = this._bindings[n];
                if (cur._listener === listener && cur.context === context) {
                    return n;
                }
            }
            return -1;
        },

        /**
         * Check if listener was attached to Signal.
         * @param {Function} listener
         * @param {Object} [context]
         * @return {boolean} if Signal has the specified listener.
         */
        has : function (listener, context) {
            return this._indexOfListener(listener, context) !== -1;
        },

        /**
         * Add a listener to the signal.
         * @param {Function} listener Signal handler function.
         * @param {Object} [listenerContext] Context on which listener will be executed (object that should represent the `this` variable inside listener function).
         * @param {Number} [priority] The priority level of the event listener. Listeners with higher priority will be executed before listeners with lower priority. Listeners with same priority level will be executed at the same order as they were added. (default = 0)
         * @return {SignalBinding} An Object representing the binding between the Signal and listener.
         */
        add : function (listener, listenerContext, priority) {
            validateListener(listener, 'add');
            return this._registerListener(listener, false, listenerContext, priority);
        },

        /**
         * Add listener to the signal that should be removed after first execution (will be executed only once).
         * @param {Function} listener Signal handler function.
         * @param {Object} [listenerContext] Context on which listener will be executed (object that should represent the `this` variable inside listener function).
         * @param {Number} [priority] The priority level of the event listener. Listeners with higher priority will be executed before listeners with lower priority. Listeners with same priority level will be executed at the same order as they were added. (default = 0)
         * @return {SignalBinding} An Object representing the binding between the Signal and listener.
         */
        addOnce : function (listener, listenerContext, priority) {
            validateListener(listener, 'addOnce');
            return this._registerListener(listener, true, listenerContext, priority);
        },

        /**
         * Remove a single listener from the dispatch queue.
         * @param {Function} listener Handler function that should be removed.
         * @param {Object} [context] Execution context (since you can add the same handler multiple times if executing in a different context).
         * @return {Function} Listener handler function.
         */
        remove : function (listener, context) {
            validateListener(listener, 'remove');

            var i = this._indexOfListener(listener, context);
            if (i !== -1) {
                this._bindings[i]._destroy(); //no reason to a SignalBinding exist if it isn't attached to a signal
                this._bindings.splice(i, 1);
            }
            return listener;
        },

        /**
         * Remove all listeners from the Signal.
         */
        removeAll : function () {
            var n = this._bindings.length;
            while (n--) {
                this._bindings[n]._destroy();
            }
            this._bindings.length = 0;
        },

        /**
         * @return {number} Number of listeners attached to the Signal.
         */
        getNumListeners : function () {
            return this._bindings.length;
        },

        /**
         * Stop propagation of the event, blocking the dispatch to next listeners on the queue.
         * <p><strong>IMPORTANT:</strong> should be called only during signal dispatch, calling it before/after dispatch won't affect signal broadcast.</p>
         * @see Signal.prototype.disable
         */
        halt : function () {
            this._shouldPropagate = false;
        },

        /**
         * Dispatch/Broadcast Signal to all listeners added to the queue.
         * @param {...*} [params] Parameters that should be passed to each handler.
         */
        dispatch : function (params) {
            if (! this.active) {
                return;
            }

            var paramsArr = Array.prototype.slice.call(arguments),
                n = this._bindings.length,
                bindings;

            if (this.memorize) {
                this._prevParams = paramsArr;
            }

            if (! n) {
                //should come after memorize
                return;
            }

            bindings = this._bindings.slice(); //clone array in case add/remove items during dispatch
            this._shouldPropagate = true; //in case `halt` was called before dispatch or during the previous dispatch.

            //execute all callbacks until end of the list or until a callback returns `false` or stops propagation
            //reverse loop since listeners with higher priority will be added at the end of the list
            do { n--; } while (bindings[n] && this._shouldPropagate && bindings[n].execute(paramsArr) !== false);
        },

        /**
         * Forget memorized arguments.
         * @see Signal.memorize
         */
        forget : function(){
            this._prevParams = null;
        },

        /**
         * Remove all bindings from signal and destroy any reference to external objects (destroy Signal object).
         * <p><strong>IMPORTANT:</strong> calling any method on the signal instance after calling dispose will throw errors.</p>
         */
        dispose : function () {
            this.removeAll();
            delete this._bindings;
            delete this._prevParams;
        },

        /**
         * @return {string} String representation of the object.
         */
        toString : function () {
            return '[Signal active:'+ this.active +' numListeners:'+ this.getNumListeners() +']';
        }

    };


    // Namespace -----------------------------------------------------
    //================================================================

    /**
     * Signals namespace
     * @namespace
     * @name signals
     */
    var signals = Signal;

    /**
     * Custom event broadcaster
     * @see Signal
     */
    // alias for backwards compatibility (see #gh-44)
    signals.Signal = Signal;



    //exports to multiple environments
    if(typeof define === 'function' && define.amd){ //AMD
        define(function () { return signals; });
    } else if (typeof module !== 'undefined' && module.exports){ //node
        module.exports = signals;
    } else { //browser
        //use string because of Google closure compiler ADVANCED_MODE
        /*jslint sub:true */
        global['signals'] = signals;
    }

}(this));

},{}],6:[function(require,module,exports){
'use strict';

// Rune
var Events = require('./global/Events');
var keys = require('./global/keys');
var Util = require('./global/Util');
var WorkSpaceController = require('./features/workspace/WorkspaceController');
var CanvasController = require('./features/canvas/CanvasController');
var DataController = require('./models/DataController');



    function App( ) {

        this.config = {
            nudge: {
                normal: 1,
                super : 10,
            }
        }

        // Setup workspace
        this.data = new DataController(Util.getLocalData("runeData"));
        this.canvas = new CanvasController(this.data);

        this.plugins = require('./plugins');

        this.workspace = new WorkSpaceController(this);

        let app = this;

        setInterval(this.save.bind(this), 20000);

    }

    Object.defineProperty(App.prototype,"save",{writable:true,configurable:true,value:function( ) {
        this.data.save();
    }});


var app = new App();

},{"./features/canvas/CanvasController":10,"./features/workspace/WorkspaceController":15,"./global/Events":17,"./global/Util":18,"./global/keys":20,"./models/DataController":22,"./plugins":30}],7:[function(require,module,exports){
'use strict';

let React = require('react');

module.exports = React.createClass({displayName: "exports",
    // getInitialState: function () {
        // debugger;
        // return null;   
    // },
    // clickHandler : function () {
    //     this.props.handler();
    // },
    click: function () {
        console.log("Button handler...");
        this.props.handler();
    },
    render: function() {
        return (
            React.createElement("div", {
                className: "button", 
                onClick: this.click}, 
                 this.props.symbol
            )
        );
    }
});

},{"react":"react"}],8:[function(require,module,exports){
'use strict';

let React = require('react');
let Button = require('./Button.jsx');

module.exports = React.createClass({displayName: "exports",
    // getInitialState: function () {
    //     return {toggle: this.props.toggle};
    // },
    // toggle: function () {
    //     this.props.onToggle();
    //     this.setState({toggle: !this.state.toggle});
    // },
    hide: function () {
        React.unmountComponentAtNode(this.props.element);
    },
    render: function () {
        return (
            React.createElement("div", {
                className: "overlay"}, 
                React.createElement("div", {className: "dialogue"}, 
                    React.createElement(Button, {handler: this.hide.bind(this), symbol: "X"}), 
                     this.props.children
                )
            )
        );
    }
});

},{"./Button.jsx":7,"react":"react"}],9:[function(require,module,exports){
'use strict';

let React = require('react');

module.exports = React.createClass({displayName: "exports",
    getInitialState: function () {
        return {toggle: this.props.toggle};
    },
    toggle: function () {
        this.props.onToggle();
        this.setState({toggle: !this.state.toggle});
    },
    render: function() {
        let classes = ("switch " + (this.state.toggle ? 'on' : 'off'));
        return (
            React.createElement("div", {
                className: classes, 
                onClick: this.toggle}, 
                 this.props.symbol
            )
        );
    }
});

},{"react":"react"}],10:[function(require,module,exports){
var GridView = require('./GridView.js');
var RuneView = require('./RuneView.js');
var Util = require('../../global/Util');
var Events = require('../../global/Events');

var paper = require('paper');

/* ========== Render Tablet ========== */


    function RuneLayer(name, view) {"use strict";
        this.name = name;
        this.view = view;
        this.layer = new paper.Layer();
    }



    function CanvasController(tabletModel) {"use strict";

    	// Canvas
        let _this = this;

        this.data = tabletModel;
    	this.canvas = document.getElementById('rune-canvas');
        this.isPreview = false;

    	paper.setup(this.canvas).install(window);
        paper.settings.handleSize = 8;

    	this.layers = [new RuneLayer('Grid')];
        this.overLayer = new RuneLayer('Overlay');

        paper.view.onResize = this.resizeHandler.bind(this);

    	// Setup grid

    	this.setupGrid();

    	tabletModel.tablet.runes.forEach(function(val, idx) {
    		_this.layers.push(
                new RuneLayer(
                    'Rune ' + idx,
                    new RuneView(val, _this.grid)
                )
            );
    	});

    	this.currentLayerIndex = 1;

    	this.showGrid = true;

        this.canvas.addEventListener('mouseDown', function(event) {
            Events.deselectAll.dispatch();
        });

        Events.redraw.add(this.drawCanvas.bind(this));
        Events.refreshCanvas.add(this.refreshCanvas.bind(this));
        Events.display.add(this.displayMode.bind(this));
        Events.redraw.dispatch();

    }

    Object.defineProperty(CanvasController.prototype,"displayMode",{writable:true,configurable:true,value:function() {"use strict";

        this.isPreview = !this.isPreview;

        this.toggleGrid(this.isPreview);

        let _this = this;

        this.layers.forEach(function(layerController, index) {
            if (index) {
                layerController.layer.children.forEach(function(child){
                    if (child.isHandle) {
                        child.visible = !_this.isPreview;
                    } else if (child.runePath) {
                        child.fillColor = _this.isPreview ? 'black' : null;
                        child.strokeColor = _this.isPreview ? null : 'red';
                        child.closed = _this.isPreview;
                    }
                });
            }
        });

        Events.redraw.dispatch();
    }});

    Object.defineProperty(CanvasController.prototype,"gridLayer",{configurable:true,get:function() {"use strict";
        return this.layers[0].layer;
    }});


	Object.defineProperty(CanvasController.prototype,"setupGrid",{writable:true,configurable:true,value:function( ) {"use strict";
		this.grid = new GridView(this.data.tablet.gridOptions);
		this.redrawGrid();
	}});

    Object.defineProperty(CanvasController.prototype,"toggleGrid",{writable:true,configurable:true,value:function(isPreview) {"use strict";
        this.showGrid = !isPreview;
        this.gridLayer.visible = this.showGrid;
        Events.redraw.dispatch();
    }});

    Object.defineProperty(CanvasController.prototype,"resizeHandler",{writable:true,configurable:true,value:function( ) {"use strict";
        this.redrawAllLayers();
    }});

    Object.defineProperty(CanvasController.prototype,"redrawAllLayers",{writable:true,configurable:true,value:function( ) {"use strict";
        let _this = this;
        this.redrawGrid();
        this.layers.forEach(function(ctrl)  {
            if (ctrl.view) {
                ctrl.layer.removeChildren();
                ctrl.layer.activate();
                ctrl.view.draw(_this.isPreview);
                ctrl.layer.translate(paper.view.center);
            }
        });
    }});

    Object.defineProperty(CanvasController.prototype,"redrawCurrentLayer",{writable:true,configurable:true,value:function( ) {"use strict";
        // Draw active layer
        let ctrl = this.layers[this.currentLayerIndex];
        ctrl.layer.removeChildren();
        ctrl.layer.activate();
        ctrl.view.draw(this.isPreview);
        ctrl.layer.translate(paper.view.center);
    }});

	Object.defineProperty(CanvasController.prototype,"redrawGrid",{writable:true,configurable:true,value:function( ) {"use strict";
		this.gridLayer.removeChildren();
		this.gridLayer.activate();
		this.grid.draw();
	}});

    Object.defineProperty(CanvasController.prototype,"refreshCanvas",{writable:true,configurable:true,value:function( ) {"use strict";
        this.setupGrid();
        this.redrawAllLayers();
        paper.view.draw();

    }});

	Object.defineProperty(CanvasController.prototype,"drawCanvas",{writable:true,configurable:true,value:function( ) {"use strict";
        this.redrawCurrentLayer();
		paper.view.draw();
	}});


module.exports = CanvasController;

},{"../../global/Events":17,"../../global/Util":18,"./GridView.js":11,"./RuneView.js":12,"paper":"paper"}],11:[function(require,module,exports){
var Util = require('../../global/util');
var constants = require('../../global/Const');
var Events = require('../../global/Events');
var RunePoint = require('../../models/RunePoint');

var paper = require('paper');


/* ========== Grid view ========== */

var gridPointFactory = function(point, res)  {

    let paperPoint = new paper.Point( point.render(res) );
    // let path = new paper.Path.Ellipse({point: paperPoint, size: [res.x, res.y]});
    let path = new paper.Path.Rectangle(paperPoint, res.x, res.y);

    path.value = point;
    path.active = false;

    var opaque = new paper.Color(255, 0, 0, 0.2);

    path.fillColor = opaque;

    path.onMouseEnter = function (e) {
        this.fillColor = 'orange';
    };

    path.onMouseLeave = function (e) {
        this.fillColor = this.active ? 'red' : opaque;
    };

    path.onMouseDown = function (e) {
        this.fillColor = 'red';
        Events.addPoint.dispatch(e.target.value);
        Events.redraw.dispatch();
    };

    return path;
}.bind(this)


	function GridView(options) {"use strict";

    	this.options = options;
    	this.points = [];

        let col, row;
        col = row = -this.options.units/2;

        for (let i = 1; i <= this.options.units * this.options.units; i++) {
            this.points.push(new RunePoint(row, col));
            if (i && i % this.options.units == 0) {
                row++;
                col = -this.options.units/2;
            } else {
                col++;
            }
        }

    }

	Object.defineProperty(GridView.prototype,"draw",{writable:true,configurable:true,value:function( ) {"use strict";

		let _this = this;

        let gridColor = new paper.Color(_this.gridColor, 100);

        let $__0=  this.options.res,x=$__0.x,y=$__0.y;

        let rowLines = new paper.Group();
        let colLines = new paper.Group();

        for (let i = -this.options.units/2; i < this.options.units/2; i++) {
            rowLines.addChild(this.xLine(i * y));
            colLines.addChild(this.yLine(i * x));
        }


        colLines.translate([paper.view.center.x + (x/2), 0]);
        rowLines.translate([0, paper.view.center.y + (y/2)]);

        this.xLine(paper.view.center.y, constants.RED);
        this.yLine(paper.view.center.x, constants.RED);

        this.createGridPoints();

	}});

    Object.defineProperty(GridView.prototype,"createGridPoints",{writable:true,configurable:true,value:function( ) {"use strict";

        this.gridPoints = new paper.Group();

        let _this = this;

        this.points.forEach(function(point)  {
            _this.gridPoints.addChild(gridPointFactory(point, this.options.res));
        }.bind(this));

        this.gridPoints.translate(paper.view.center);
    }});

	Object.defineProperty(GridView.prototype,"yLine",{writable:true,configurable:true,value:function(xLoc, color) {"use strict";
		let line = new paper.Path.Rectangle([xLoc, 0], 1, 2000);
		line.fillColor = color ? color : constants.BLUE;
        return line;
	}});

	Object.defineProperty(GridView.prototype,"xLine",{writable:true,configurable:true,value:function(yLoc, color) {"use strict";
		let line = new paper.Path.Rectangle([0, yLoc], 2000, 1);
		line.fillColor = color ? color : constants.BLUE;
        return line;
	}});


module.exports = GridView;

},{"../../global/Const":16,"../../global/Events":17,"../../global/util":21,"../../models/RunePoint":25,"paper":"paper"}],12:[function(require,module,exports){
var Events = require('../../global/Events');
var paper = require('paper');

/* ========== Tablet ========== */


    function _this(runeModel, grid) {"use strict";
        this.data = runeModel;
        this.grid = grid.options;
    }

    Object.defineProperty(_this.prototype,"draw",{writable:true,configurable:true,value:function(isPreview) {"use strict";

        var $_this_this = this;

        this.paths = $_this_this.data.paths.map(function(path)  {
            let style = (path.isClosed ? {fillColor: 'black'} : {strokeColor: path.isActive ? 'red' :'black'});
            let paperPath;
            if (path.hasChildren) {
                let paths = [path].concat(path.children);

                paperPath = new paper.CompoundPath({
                    children: paths.map(function(p)  {
                        return new paper.Path({
                            segments: $_this_this.getPathSegment(p, isPreview)
                        });
                    }),
                    style: style
                })
            } else {
                paperPath = new paper.Path({
                    segments: this.getPathSegment(path, isPreview),
                    style: style,
                    closed: path.isClosed
                });
            }

            paperPath.opacity = 0.6;

            return paperPath;
        }.bind(this));
    }});

    Object.defineProperty(_this.prototype,"getPathSegment",{writable:true,configurable:true,value:function(path, isPreview) {"use strict";
        let _this = this;
        return path.points.map(function(point) {
            return _this.createRuneSegment(
                point,
                null,
                isPreview
            );
        })
    }});

    Object.defineProperty(_this.prototype,"createRuneSegment",{writable:true,configurable:true,value:function(point, transform, isPreview) {"use strict";

        let segment;

        let renderedPoint = new paper.Point(
            point.render(this.grid.res)
        ).add(
            new paper.Point(this.grid.res.x/2, this.grid.res.y/2)
        );

        // if(point.transforms) {
        //     point.transforms.forEach((transform) => {
        //         renderedPoint = renderedPoint.add(new paper.Point(
        //             transform[0] * this.grid.res.x,
        //             transform[1] * this.grid.res.y
        //         ));
        //     });
        // }

        if(point.transform) {
            renderedPoint = renderedPoint.add(new paper.Point(
                point.transform[0] * this.grid.res.x,
                point.transform[1] * this.grid.res.y
            ));
        }

        let h1 = point.handle1 ? new paper.Point(point.handle1[0], point.handle1[1]) : null; 
        let h2 = point.handle2 ? new paper.Point(point.handle2[0], point.handle2[1]) : null; 


        if (point.isCurve) {
            segment = new paper.Segment({
                point: renderedPoint,
                handleIn: h1,
                handleOut: h2
            });
        }

        if (!isPreview) {

            let node = new paper.Path.Circle(renderedPoint, 8);
            node.isHandle = true;
            node.fillColor = 'white';
            node.value = point;
            node.isSelected = point.isSelected || false;
            node.strokeWidth = 4;
            node.strokeColor = node.isSelected ? 'red' : false;

            node.onMouseDown = function(e) {
                e.event.stopImmediatePropagation();
                this.isSelected = !this.isSelected;
                Events.selectPoint.dispatch(e.target.value);
                Events.redraw.dispatch();
            }

            if (point.isCurve) {

                if (point.handle1) {
                    let h1p = new paper.Path.Circle(renderedPoint.add(h1), 5);
                    h1p.strokeColor = 'red';
                    let p1 = new paper.Path.Line(renderedPoint.add(h1), renderedPoint);
                    p1.strokeColor = 'red';
                }

                if (point.handle2) {
                    let h2p = new paper.Path.Circle(renderedPoint.add(h2), 5);
                    h2p.strokeColor = 'red';
                    let p2 = new paper.Path.Line(renderedPoint.add(h2), renderedPoint);
                    p2.strokeColor = 'red';
                }
            }

        }

        return segment || renderedPoint;
    }});


module.exports = _this;

},{"../../global/Events":17,"paper":"paper"}],13:[function(require,module,exports){
'use strict';

var Util = require('../../global/Util');
var React = require('react');
var Draggable = require('react-draggable');

var React = require('react');
var Events = require('../../global/Events');



    function PanelController(app) {
        this.app = app;
        this.init();
    }

    Object.defineProperty(PanelController.prototype,"init",{writable:true,configurable:true,value:function( ) {

        var $PanelController_this = this;

        var PanelWrapper = React.createClass({displayName: "PanelWrapper",
            handleStart: function () {

            },
            handleDrag: function () {

            },
            toggleShow: function () {
                this.setState({collapsed: !this.state.collapsed});
            },
            getInitialState: function () {
                return { collapsed : this.props.options.collapsed };
            },
            render: function() {
                var offsetY = this.props.offset * 50;
                var offsetX = this.props.offset * 5;
                return (
                    React.createElement("div", {className: "panel"}, 
                        React.createElement("div", {className: "handle", onClick: this.toggleShow}, 
                             this.props.options.title, 
                            React.createElement("span", {className: "toggle"}, this.state.collapsed ? '-' : '+')
                        ), 
                         !this.state.collapsed ? 
                        React.createElement("div", {className: "panel-content"}, 
                             this.props.children
                        ) : null
                    )
                );
            }
        });

        var Panels = React.createClass({displayName: "Panels",
            getInitialState: function () {
                return { panels : this.props.panels, data : this.props.data, canvas: this.props.canvas };
            },
            componentWillReceiveProps: function (nextProps) {
                return nextProps;
            },
            render: function() {
                let $PanelController_this = this;
                return (
                    React.createElement("div", null, 
                        
                            this.state.panels.map(function(panel, idx) {
                                var Component = panel.panel;
                                return React.createElement(PanelWrapper, {offset: idx, options: {title : panel.title, collapsed: panel.collapsed}}, 
                                     React.createElement(Component, {data: $PanelController_this.state.data, canvas: $PanelController_this.state.canvas})
                                 );
                            })
                        
                    )
                );
            }
        });

        // Plugin panels
        let panels = React.render(
            React.createElement(Panels, {
                panels: $PanelController_this.app.plugins, 
                data: $PanelController_this.app.data, 
                canvas: $PanelController_this.app.canvas}),
            document.getElementById('rune-panels')
        );

        function reloadHandler () {
            console.log("Reloading panels...");
            panels.replaceState({'data' : $PanelController_this.app.data, 'panels' : $PanelController_this.app.plugins, canvas: $PanelController_this.app.canvas });
        };

        Events.reloadPanels.add(reloadHandler.bind($PanelController_this));

        Events.refreshPanels.add(function()  {
            console.log("Refreshing panels...");
            panels.setState({'data' : $PanelController_this.app.data});
        });

    }});



// <Draggable
//     start={{x: offsetX + 800, y: offsetY }}
//     onStart={this.handleStart}
//     handle=".handle"
//     >
//     <div className="panel">
//         <div className="handle">{ this.props.options.title }<span className="toggle">-</span></div>
//         <div className="panel-content">
//             { this.props.children }
//         </div>
//     </div>
// </Draggable>


module.exports = PanelController;

},{"../../global/Events":17,"../../global/Util":18,"react":"react","react-draggable":1}],14:[function(require,module,exports){
'use strict';

let Events = require('../../global/Events');

let React = require('react');
let paper = require('paper');

// React components:
let Dialogue = require('../../components/Dialogue.jsx');
let Button = require('../../components/Button.jsx');

function ActionBar(app) {

	var actionBar = this;
	actionBar.app = app;

	this.actions = [
		{
			id : "save",
			title : "Save",
			action : function(e) {
				e.preventDefault();
				// debugger;
				actionBar.app.data.save();

			}
		},
		{
			id : "clear",
			title : "Clear",
			action: function(e) {
				e.preventDefault();
				// Util.dispatchRuneEvent("clearGridPoints");
                Events.clearPoints.dispatch();
			}
		},
		{
			id: "grid",
			title: "Preview",
			action: function(e) {
				e.preventDefault();
                Events.display.dispatch();
			}
		},
		{
			id: "rune",
			title: "Add rune",
			action: function(e) {
				e.preventDefault();
				app.tablet.addRune();
			}
		},
		{
			id: "svg",
			title: "Export as SVG",
			action: function(e) {
				e.preventDefault();
                actionBar.app.canvas.displayMode = 'preview';
                console.log("here");
				let svgString = paper.project.exportSVG({asString:true, layerIndex: 2});
                let element = document.getElementById('rune-overlay');
                let dialogue = React.render(
                    React.createElement(Dialogue, {
                        element: element}, 
                        React.createElement("h2", null, "SVG code:"), 
                        React.createElement("textarea", null,  svgString )
                    ),
                    element
                );
				// var url = "data:image/svg+xml;utf8," + encodeURIComponent(svgString);
				// var link = document.createElement("a");
    //             console.log("and here");
				// link.download = 'rune_export.svg';
				// link.href = url;
				// link.click();
    //             actionBar.app.canvas.displayMode = 'working';
			}
		}
	];
	this.render();
	this.addEvents();
}

ActionBar.prototype.addEvents = function(container) {
	for (var i=0; i < this.actions.length; i++) {
		var action = this.actions[i];
		document.querySelectorAll('[data-action="' + action.id + '"]')[0].onclick = action.action;
	}
};

ActionBar.prototype.render = function() {
	console.log("this is working");

	var actions = this.actions;

	var ActionBarComponent = React.createClass({displayName: "ActionBarComponent",
	    render: function() {
	        return (
				React.createElement("ul", null, 
					
						this.props.actions.map(function(action, i) {
							return React.createElement("li", null, React.createElement("a", {className: "action", "data-action":  action.id},  action.title));
						})
					
				)
	        );
	    }
	});

	React.render(
	    React.createElement(ActionBarComponent, {actions: actions}),
	    document.getElementById('rune-actionbar')
	);
};

module.exports = ActionBar;

},{"../../components/Button.jsx":7,"../../components/Dialogue.jsx":8,"../../global/Events":17,"paper":"paper","react":"react"}],15:[function(require,module,exports){
var ActionBar = require('./ActionBarController.jsx');
var PanelController = require('./../panels/PanelController.jsx');

function WorkSpaceController(app) {

	this.actionBar = new ActionBar(app);
	this.panels = new PanelController(app);
}

WorkSpaceController.prototype = {
	constructor: WorkSpaceController,
}

module.exports = WorkSpaceController;

},{"./../panels/PanelController.jsx":13,"./ActionBarController.jsx":14}],16:[function(require,module,exports){
module.exports = {
	SILVER_RATIO : Math.sqrt(2),
	GOLDEN_RATIO :  (1 + Math.sqrt(5)) / 2,
	CANVAS_SIZE : 280,
	BLUE:  '#41d4f3',
    RED: '#ff0000'
};

},{}],17:[function(require,module,exports){
let signals = require('signals');

let events = [
    'addPoint',
    'addPath',
    'addSubPath',
    'selectPoint',
    'selectPath',
    'deleteSelected',
    'deletePoint',
    'clearPoints',
    'drawToOverlay',
    'superNudge',
    'nudge',
    'display',
    'deselectAll',
    'refreshCanvas',
    'redraw',
    'reloadPanels',
    'refreshPanels',
    'zoomIn',
    'zoomOut'
];

Events = {};

events.forEach(function(event)  {
    Events[event] = new signals.Signal();
});

module.exports = Events;

},{"signals":5}],18:[function(require,module,exports){
/* ========== Utilities ========== */

function Util(){"use strict";}
	Object.defineProperty(Util,"getIndices",{writable:true,configurable:true,value:function(points, gridPoints) {"use strict";
		return points.map(function(point) {
			return gridPoints.indexOf(point);
		});
	}});

	Object.defineProperty(Util,"object",{writable:true,configurable:true,value:function(o) {"use strict";
        function F() {}
        F.prototype = o;
        return new F();
    }});

    Object.defineProperty(Util,"getLocalData",{writable:true,configurable:true,value:function(ref) {"use strict";
    	return (localStorage[ref] && typeof localStorage[ref] === 'string') ? JSON.parse(localStorage[ref]) : null;
    }});


module.exports = Util;

},{}],19:[function(require,module,exports){
module.exports = {
	SILVER_RATIO : Math.sqrt(2),
	GOLDEN_RATIO :  (1 + Math.sqrt(5)) / 2,
	CANVAS_SIZE : 280,
	BLUE:  '#41d4f3',
    RED: '#ff0000'
};

},{}],20:[function(require,module,exports){
'use strict';

let Events = require('./Events');

const MODIFIERS = [
    'shiftKey',
    'ctrlKey'
];

const keyMap = {
    delete: 8,
    tab: 9,
    enter: 13,
    esc: 27,
    space: 32,
    left: 37,
    up: 38,
    right: 39,
    down: 40,
    p: 80
};

var superNudgeVectors = {
    'up' : [0,-1],
    'down': [0,1],
    'left' : [-1,0],
    'right' : [1,0]
};

var nudgeVectors = {
    'up' : [0,-0.1],
    'down': [0,0.1],
    'left' : [-0.1,0],
    'right' : [0.1,0]
};


let Keys = {
    key: keyMap,
    maps: {},
    mapKey:function (key, callback) {
        if (this.maps[key]) {
            throw new Error('Key already mapped, soz bro.');
        }
        this.maps[key] = callback;
    },
    init:function () {
        let _this = this;
        document.addEventListener('keydown', function(e) {
            let hasModifier = MODIFIERS.find(function(mod)  {return e[mod];});

            let ref = ((hasModifier && hasModifier + '+' || '') + e.keyCode);

            console.log(ref);

            if (_this.maps[ref] && e.target.tagName !== 'INPUT') {
                e.preventDefault();
                _this.maps[ref]();
            }
        });

        this.mapKey(this.key.delete, function()  {
            Events.deleteSelected.dispatch();
        });

        this.mapKey(this.key.p, function()  {
            Events.display.dispatch();
        });

        Object.keys({
            'up' : this.key.up,
            'down' : this.key.down,
            'left' : this.key.left,
            'right' : this.key.right,
        }).forEach(function(key)  {
            Keys.mapKey(("shiftKey+" + Keys.key[key]), function()  {
                Events.nudge.dispatch(superNudgeVectors[key]);
            });
            Keys.mapKey((Keys.key[key]), function()  {
                Events.nudge.dispatch(nudgeVectors[key]);
            });
        });

        return this;
    }
};

module.exports = Keys.init();

},{"./Events":17}],21:[function(require,module,exports){
/* ========== Utilities ========== */

function Util(){"use strict";}
	Object.defineProperty(Util,"getIndices",{writable:true,configurable:true,value:function(points, gridPoints) {"use strict";
		return points.map(function(point) {
			return gridPoints.indexOf(point);
		});
	}});

	Object.defineProperty(Util,"object",{writable:true,configurable:true,value:function(o) {"use strict";
        function F() {}
        F.prototype = o;
        return new F();
    }});

    Object.defineProperty(Util,"getLocalData",{writable:true,configurable:true,value:function(ref) {"use strict";
    	return (localStorage[ref] && typeof localStorage[ref] === 'string') ? JSON.parse(localStorage[ref]) : null;
    }});


module.exports = Util;

},{}],22:[function(require,module,exports){
var TabletModel = require('./TabletModel');
var RuneModel = require('./RuneModel');
var Events = require('../global/Events');
var _ = require('lodash');

/* ========== Data ========== */


    function DataController(tabletModel) {"use strict";

        this.tablet = new TabletModel(tabletModel);
        this.currentRune = 0;

        Events.addPoint.add(this.addPoint.bind(this));
        Events.selectPoint.add(this.selectPoint.bind(this));
        Events.selectPath.add(this.selectPath.bind(this));
        Events.clearPoints.add(this.clearRune.bind(this));
        Events.addPath.add(this.addPath.bind(this));
        Events.deletePoint.add(this.deletePoint.bind(this));
        Events.addSubPath.add(this.addSubPath.bind(this));

        Events.nudge.add(this.addTransformToSelected.bind(this));

    }

    Object.defineProperty(DataController.prototype,"activeRune",{configurable:true,get:function( ) {"use strict";
        return this.tablet.runes[this.currentRune];
    }});

    Object.defineProperty(DataController.prototype,"save",{writable:true,configurable:true,value:function( ) {"use strict";
        localStorage["runeData"] = JSON.stringify(this.tablet);
    }});

    Object.defineProperty(DataController.prototype,"addRune",{writable:true,configurable:true,value:function( ) {"use strict";
        this.tablet.runes.push(new RuneModel(null));
    }});

    Object.defineProperty(DataController.prototype,"addPoint",{writable:true,configurable:true,value:function(gridRef) {"use strict";
        this.activeRune.addPoint(gridRef);
        Events.deselectAll.dispatch();
    }});

    Object.defineProperty(DataController.prototype,"addPath",{writable:true,configurable:true,value:function( ) {"use strict";
        this.activeRune.addPath();
        Events.redraw.dispatch();
    }});

    Object.defineProperty(DataController.prototype,"addSubPath",{writable:true,configurable:true,value:function(path) {"use strict";
        this.activeRune.addSubPath(path);
        Events.redraw.dispatch();
    }});

    Object.defineProperty(DataController.prototype,"addTransformToSelected",{writable:true,configurable:true,value:function(transform) {"use strict";

        console.log(transform);

        // this.activeRune.selectedPoints.forEach((point) => {
        //     point.transform[0] += transform[0],
        //     point.transform[1] += transform[1]
        // });

        this.activeRune.selectedPoint.x += transform[0];
        this.activeRune.selectedPoint.y += transform[1];

        // this.activeRune.selectedPoints.forEach((point) => {
        //     point.transforms.push(transform);
        // });
        Events.redraw.dispatch();
    }});

    Object.defineProperty(DataController.prototype,"clearRune",{writable:true,configurable:true,value:function() {"use strict";
        this.activeRune.clearPaths().currentPointIndex = 0;
        Events.reloadPanels.dispatch();
        Events.redraw.dispatch();
    }});

    Object.defineProperty(DataController.prototype,"updateGrid",{writable:true,configurable:true,value:function( ) {"use strict";
        // var rune = this.getactiveRune();
        // rune.letter.gridPoints.forEach(function(entry, i) {
        // });
    }});

    Object.defineProperty(DataController.prototype,"selectPoint",{writable:true,configurable:true,value:function(isSelected, point) {"use strict";
        this.activeRune.selectHandler(isSelected, point);
    }});

    Object.defineProperty(DataController.prototype,"selectPath",{writable:true,configurable:true,value:function(path) {"use strict";
        this.activeRune.selectPath(path);
        Events.refreshPanels.dispatch();
        Events.redraw.dispatch();
    }});

    Object.defineProperty(DataController.prototype,"deletePoint",{writable:true,configurable:true,value:function(p) {"use strict";
        this.activeRune.deletePoint(p);
    }});


module.exports = DataController;

},{"../global/Events":17,"./RuneModel":24,"./TabletModel":26,"lodash":"lodash"}],23:[function(require,module,exports){
let constants = require('../global/const');

const DEFAULT_UNITS = 10;



    function GridModel(data) {"use strict";
        this.units = data && data.units || (DEFAULT_UNITS % 2 == 0 ? DEFAULT_UNITS : DEFAULT_UNITS++);
        this.ratio = data && data.ratio || 1;
        this.baseUnit = data && data.baseUnit || (constants.CANVAS_SIZE / DEFAULT_UNITS);
    }

    Object.defineProperty(GridModel.prototype,"res",{configurable:true,get:function( ) {"use strict";
        return {
            x: this.baseUnit,
            y: this.baseUnit * this.ratio 
        };
    }});


module.exports = GridModel;

},{"../global/const":19}],24:[function(require,module,exports){
var RunePoint = require('./RunePoint');

/* ========== Rune Model ========== */


    function RunePathModel(data) {"use strict";
        this.points = data && data.points.map(function(p)  {return new RunePoint(p);}) || [];
        this.isClosed = data && data.isClosed || false;
        this.isActive = data && data.isActive || false;
        this.children = data && data.children || [];
        this.children.map(function(c)  {return new RunePathModel(c);});
    }

    Object.defineProperty(RunePathModel.prototype,"addChild",{writable:true,configurable:true,value:function(path) {"use strict";
        this.children.push(path);
    }});

    Object.defineProperty(RunePathModel.prototype,"hasChildren",{configurable:true,get:function( ) {"use strict";
        return !!this.children.length; 
    }});



    function RuneModel(data) {"use strict";

        this.paths = data && data.paths.map(function(p)  {return new RunePathModel(p);}) || [new RunePathModel()];
        this.activePath = this.paths[0];
        this.selectedPoints = data && data.selectedPoints || [];
        this.currentPointIndex = data && data.currentPointIndex || 0;
        this.reverseAdd = false;
        this.selectedPoint = null;

        // Events.
    }

    Object.defineProperty(RuneModel.prototype,"clearPaths",{writable:true,configurable:true,value:function( )  {"use strict";
        this.paths = [new RunePathModel()];
        return this;
    }});

    Object.defineProperty(RuneModel.prototype,"selectHandler",{writable:true,configurable:true,value:function(point) {"use strict";
        point.isSelected = !point.isSelected;
        if(point.isSelected) {
            this.selectedPoint = point;
            console.log("Selectedpoint", this.selectedPoint);
            // this.currentPointIndex = point.idx; ???
        } else { 
            this.selectedPoint = null;
            // this.selectedPoints.forEach((p, i) => {
                // if (point == p) {
                    // this.selectedPoints.splice(i, 1);
                // }
            // });
        }
        Events.refreshPanels.dispatch();
        Events.redraw.dispatch();
    }});

    Object.defineProperty(RuneModel.prototype,"addPath",{writable:true,configurable:true,value:function( ) {"use strict";
        let path = new RunePathModel();
        path.isActive = true;
        this.paths.push(path);
        this.activePath = path;
        Events.refreshPanels.dispatch();
    }});

    Object.defineProperty(RuneModel.prototype,"addSubPath",{writable:true,configurable:true,value:function(path) {"use strict";
        let subPath = new RunePathModel();
        subPath.isActive = true;
        path.addChild(subPath);
        this.activePath = subPath;
        Events.refreshPanels.dispatch();
    }});

    // addChildPath () {
        // this.activePath.addChild(new RunePathModel());
    // }

    Object.defineProperty(RuneModel.prototype,"selectPath",{writable:true,configurable:true,value:function(path) {"use strict";
        path.isActive = true;
        this.activePath.isActive = false;
        this.activePath = path;
    }});

    Object.defineProperty(RuneModel.prototype,"addPoint",{writable:true,configurable:true,value:function(gridPoint) {"use strict";
        // if (this.selectedPoints.length) {
        //     if (this.selectedPoints[0] == 0 ) {
        //         this.reverseAdd = true;
        //     } else if (this.selectedPoints[0] == this.activePath.length-1) {
        //         this.reverseAdd = false;
        //     }
        // }
        // if(this.reverseAdd) {
        //     this.activePath.points.unshift(new RunePoint(gridRef.x, gridRef.y));
        // } else {
        //     this.currentPointIndex++;
        //     this.activePath.points.splice(this.currentPointIndex, 0, new RunePoint(gridRef.x, gridRef.y));
        // }

        let point = new RunePoint(gridPoint.x, gridPoint.y);

        point.gridPoint = gridPoint;

        this.activePath.points.push(point);
        
        Events.refreshPanels.dispatch();

        return this;
    }});

    Object.defineProperty(RuneModel.prototype,"currentPoint",{configurable:true,get:function() {"use strict";
        return this.paths[this.activePathIndex][this.currentPointIndex];
    }});

    Object.defineProperty(RuneModel.prototype,"currentPoint",{configurable:true,set:function(arr) {"use strict";
        this.activePathIndex = arr[0];
        this.currentPointIndex = arr[1];
    }});

    Object.defineProperty(RuneModel.prototype,"deleteSelected",{writable:true,configurable:true,value:function( ) {"use strict";
        // FIX THIS
        // var rune = this.activeRune;
        // this.activePath = this.activePath.filter(function(value, idx) {
            // return !~rune.selectedPoints.indexOf(idx);
        // });
    }});

    Object.defineProperty(RuneModel.prototype,"deletePoint",{writable:true,configurable:true,value:function(p) {"use strict";

        debugger;

        this.activePath.points.forEach(function(point, i)  {
            if (point === p) {
                this.activePath.points.splice(i, 1);
            }
        }.bind(this));
        Events.redraw.dispatch();
        Events.refreshPanels.dispatch();
    }});

       
           
     

       
          
     


       
          
     

       
          
     

       
           
     


module.exports = RuneModel;

},{"./RunePoint":25}],25:[function(require,module,exports){
/* ========== Point Model ========== */


    function BasePoint(x, y) {"use strict";
        this.x = x;
        this.y = y;
    }


for(var BasePoint____Key in BasePoint){if(BasePoint.hasOwnProperty(BasePoint____Key)){RunePoint[BasePoint____Key]=BasePoint[BasePoint____Key];}}var ____SuperProtoOfBasePoint=BasePoint===null?null:BasePoint.prototype;RunePoint.prototype=Object.create(____SuperProtoOfBasePoint);RunePoint.prototype.constructor=RunePoint;RunePoint.__superConstructor__=BasePoint;

    function RunePoint(x, y) {"use strict";
        if (typeof x === 'object') {
            BasePoint.call(this,x.x, x.y);
            this.transforms = x.transforms || [];
            this.transform = x.transform || [0,0];
            this.handle1 = x.handle1 || null;
            this.handle2 = x.handle2 || null;
            this.isCurve = x.isCurve || false;
            this.isSelected = x.isSelected || false;
        } else {
            BasePoint.call(this,x, y);
            this.transforms = [];
            this.transform = [0,0];
            this.handle1 = null;
            this.handle2 = null;
            this.isCurve = false;
            this.isSelected = false
        }
    }

    Object.defineProperty(RunePoint.prototype,"addHandles",{writable:true,configurable:true,value:function( ) {"use strict";
        this.handles = [new BasePoint(), new BasePoint()];
    }});

    Object.defineProperty(RunePoint.prototype,"render",{writable:true,configurable:true,value:function(res) {"use strict";
        return [
            this.x * res.x, 
            this.y * res.y
        ]; 
    }});


module.exports = RunePoint;

},{}],26:[function(require,module,exports){
let RuneModel = require('./RuneModel');
let GridModel = require('./GridModel');

/* ========== Tablet Model ========== */


    function TabletModel(data) {"use strict";

        let _this = this;

        _this.gridOptions = new GridModel(data && data.gridOptions);

        _this.renderedSVG = data && data.renderedSVG || '';
        _this.activePathIndex = data && data.activePathIndex || 0;

        _this.runes = data && data.runes.map(function(rune)  {return new RuneModel(rune);}) || [new RuneModel()];
    }


module.exports = TabletModel;

},{"./GridModel":23,"./RuneModel":24}],27:[function(require,module,exports){
'use strict';

let React = require('react');
let Events = require('../global/Events');

let Button = require('../components/Button.jsx');

module.exports = {
    title: 'Grid',
    collapsed: true,
    panel: React.createClass({displayName: "panel",
        getInitialState : function() {
            return {data: this.props.data.tablet.gridOptions };
        },
        updateData: function(e) {
            let baseUnit = this.refs.baseUnit.getDOMNode().value;
            let ratio = this.refs.ratio.getDOMNode().value;
            let units = this.refs.units.getDOMNode().value;
            this.state.data.baseUnit = +baseUnit;
            this.state.data.ratio = +ratio;
            this.state.data.units = +units;
            Events.refreshCanvas.dispatch();
        },
        render: function() {
            return (
                React.createElement("div", null, 
                    React.createElement("label", null, "Zoom level"), 
                    React.createElement("input", {type: "text", ref: "baseUnit", defaultValue:  this.state.data.baseUnit}), 
                    React.createElement("label", null, "Ratio"), 
                    React.createElement("input", {type: "text", ref: "ratio", defaultValue:  this.state.data.ratio}), 
                    React.createElement("label", null, "Units"), 
                    React.createElement("input", {type: "text", ref: "units", defaultValue: this.state.data.units}), 
                    React.createElement("button", {onClick: this.updateData}, 
                        "Update"
                    )
                )
            );
        }
    })
};

},{"../components/Button.jsx":7,"../global/Events":17,"react":"react"}],28:[function(require,module,exports){
'use strict';

let React = require('react');
let Events = require('../global/Events');
let Switch = require('../components/Switch.jsx');
let Button = require('../components/Button.jsx');

let PointData = React.createClass({displayName: "PointData",
    getInitialState: function () {
        return {point: this.props.point};
    },
    setIsCurve: function (point) {
        point.isCurve = !point.isCurve;
        this.setState({point: point});
        Events.redraw.dispatch();
    },
    toggleActive : function () {
        Events.selectPoint.dispatch(this.state.point);
        this.setState({point: this.state.point});
    },
    componentWillReceiveProps : function (nextProps) {
      return nextProps;
    },
    changeHandle: function (ref, event) {
        let coords = event.target.value.split(',');
        let p = this.state.point;
        p[ref] = coords.length === 2 ? coords.map(function(c)  {return +c;}) : null;
        this.setState({point: p});
        Events.redraw.dispatch();
    },
    deletePoint: function(point)  {
        Events.deletePoint.dispatch(point);
    },
    render: function() {
        let x = this.props.point.x;
        let y = this.props.point.y;
        let classNames = this.state.point.isSelected ? 'sheet active' : 'sheet';
        return (
            React.createElement("div", {
                className: classNames}, 
                React.createElement("span", {
                    onClick: this.toggleActive}, 
                    "[•]"
                ), 
                React.createElement("small", null, 
                    React.createElement("span", null, "x: ", x, ", y:", y)
                ), 
                this.state.point.transforms, 
                React.createElement(Switch, {
                    onToggle: this.setIsCurve.bind(this, this.state.point), 
                    toggle: this.state.point.isCurve, 
                    symbol: "S"}
                ), 
                React.createElement(Button, {
                    handler: this.deletePoint.bind(this, this.state.point), 
                    symbol: "X"}
                ), 
                
                    this.state.point.isCurve ?
                        React.createElement("div", null, 
                            "Handles", React.createElement("br", null), 
                            React.createElement("input", {
                                type: "text", 
                                defaultValue: this.state.point.handle1, 
                                onChange: this.changeHandle.bind(this, 'handle1')}), 
                            React.createElement("input", {
                                type: "text", 
                                defaultValue: this.state.point.handle2, 
                                onChange: this.changeHandle.bind(this, 'handle2')})
                        )
                    : null
                
            )
        );
    }
});

let Path = React.createClass({displayName: "Path",
    getInitialState: function () {
        return {path: this.props.path};
    },
    changeHandler: function () {
        this.state.path.isClosed = !this.state.path.isClosed;
        this.setState({path: this.state.path});
        Events.redraw.dispatch();
    },
    componentWillReceiveProps : function (nextProps) {
        this.setState({path :nextProps.path });
        return nextProps;
    },
    selectPath : function (path) {
        if (!this.state.path.isActive) {
            Events.selectPath.dispatch(path);
        }
    },
    addSubPath: function(path)  {
        Events.addSubPath.dispatch(path);
    },
    render: function () {
        let classNames = this.state.path.isActive ? 'sheet active' : 'sheet';
        return (
            React.createElement("div", {className: classNames}, 
                React.createElement("span", {onClick: this.selectPath.bind(this, this.state.path)}, 
                    React.createElement("strong", null, 
                        "Path"
                    )
                ), 
                React.createElement(Button, {
                    handler: this.addSubPath.bind(this, this.state.path), 
                    symbol: "+S"}
                ), 
                React.createElement(Switch, {onToggle: this.changeHandler, symbol: "&"}), 
                 
                    this.state.path.hasChildren ?
                        this.state.path.children.map(function(p)  {
                            return React.createElement(Path, {path: p});
                        })
                    : null, 
                
                 this.state.path.points.map(function(p)  {return React.createElement(PointData, {point: p});}) 
            )
        );
    }
});

module.exports = {
    title: 'Inspect path',
    collapsed: false,
    panel: React.createClass({displayName: "panel",
        getInitialState: function () {
            return {paths: this.props.data.activeRune.paths};
        },
        componentWillReceiveProps: function(nextProps) {
            this.setState({
                paths: nextProps.data.activeRune.paths
            });
        },
        addPath: function()  {
            Events.addPath.dispatch();
        },
        render: function() {
            let _this = this;
            return (
                React.createElement("div", null, 
                    React.createElement(Button, {
                        handler: this.addPath.bind(this), 
                        symbol: "+P"}
                    ), 
                    
                        this.state.paths.map(function(path)  {
                            return React.createElement(Path, {path: path})
                        })
                    
                )
            );
        }
    })
};

},{"../components/Button.jsx":7,"../components/Switch.jsx":9,"../global/Events":17,"react":"react"}],29:[function(require,module,exports){
'use strict';

let React = require('react');
let paper = require('paper');

module.exports = {
    title: 'Layers',
    collapsed: false,
    panel: React.createClass({displayName: "panel",
        getInitialState : function() {
            return {layers: this.props.canvas.layers };
        },
        render: function() {
            return (
                React.createElement("div", null, 
                    React.createElement("h4", null, "Layers"), 
                    React.createElement("div", null, 
                        
                            this.state.layers.map(function(layer) {
                                return React.createElement("div", {className: "sheet"}, 
                                    layer.name
                                )
                            })
                        
                    )
                )
            );
        }
    })
};

},{"paper":"paper","react":"react"}],30:[function(require,module,exports){
module.exports = [
	require('./InspectPath.jsx'),
    require('./GridManager.jsx'),
    require('./LayerManager.jsx')
    // require('./Nudge.jsx')
    // require('./PointRandomise.jsx'),
    // require('./Shapes.jsx'),
    // require('./FourPointWeightTransform.jsx')
];

},{"./GridManager.jsx":27,"./InspectPath.jsx":28,"./LayerManager.jsx":29}]},{},[6]);
