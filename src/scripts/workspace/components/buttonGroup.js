'use strict';

import React from 'react';

export default function (props) {
    return (
        <div
            className="button-group">
            { this.props.children }
        </div>
    );
}
